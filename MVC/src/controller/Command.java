package controller;
/**
 * 
 *command  interface
 */
public interface Command {
	/**
	 * doing the command
	 * @param args the template array objects
	 */
	void doCommand(String[] args);
}
